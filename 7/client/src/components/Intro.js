import React from 'react';
import '../App.css';

const Intro = () => {
  return (
    <div>
      <p>
        This is a simple example of using React to Query the Pokemon API. 
        You can choose different trainers to catch and release pokemons.
      </p>

      
    </div>
  );
};

export default Intro;
